{
  "name": "quickli-monorepo",
  "version": "0.0.1",
  "private": true,
  "description": "quiCkLI monorepo — Python CLI framework and documentation",
  "engines": {
    "node": ">=24",
    "pnpm": ">=11"
  },
  "scripts": {
    "test": "pnpm run test:core",
    "test:core": "pnpm --filter core run test",
    "lint:core": "pnpm --filter core run lint",
    "format:check:core": "pnpm --filter core run format:check",
    "coverage:core": "pnpm --filter core run coverage",
    "build:core": "pnpm --filter core run build",
    "build:docs": "pnpm --filter quickli-docs run build",
    "start:docs": "pnpm --filter quickli-docs run start"
  },
  "packageManager": "pnpm@11.17.0"
}